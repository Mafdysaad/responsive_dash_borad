class Transaction_Model {
  String title, date, price;
  bool iswithdrawal;
  Transaction_Model(
      {required this.date,
      required this.title,
      required this.price,
      required this.iswithdrawal});
}
